<resources>

    <!-- Base application theme -->
    <style name="Base.Theme.PoojaPurohit" parent="Theme.Material3.Light.NoActionBar" >
    <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- Main app theme -->
    <style name="Theme.PoojaPurohit" parent="Base.Theme.PoojaPurohit" >
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:fontFamily">serif</item>
    </style>

</resources>
