<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Required on Android 13+ (API 33) to show push notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <queries>
        <package android:name="com.google.android.gms" />
    </queries>

    <application
        android:allowBackup="true"
        android:name=".bookpurohit.BookPurohitApplication"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PoojaPurohit"
        tools:targetApi="31">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}"/>

        <!-- FCM default notification icon — used when app is in background/killed state -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/logo" />

        <!-- Accent colour applied to the notification icon on Android 5+ -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/ic_launcher_background" />


        <!--
            FCM Messaging Service.
            Handles two cases:
              - onNewToken: token rotation → updates Firestore fcmTokens[]
              - onMessageReceived: foreground push display
            Background/killed state is handled automatically by the FCM SDK
            using the `notification` block sent by the Cloud Function.
        -->
        <service
            android:name=".notification.PoojaMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity
            android:name=".splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.PoojaPurohit.MySplash">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".auth.compose.AuthActivity"
            android:exported="true"
            android:theme="@style/Theme.PoojaPurohit"
            android:windowSoftInputMode="adjustResize">
        </activity>

        <activity
            android:name="com.poojapurohit.dashboard.DashActivity"
            android:exported="true"
            android:theme="@style/Theme.PoojaPurohit">
        </activity>

        <activity
            android:name=".dashboard.EditProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.PoojaPurohit">
        </activity>

        <activity
            android:name=".notification.compose.NotificationActivity"
            android:exported="false"
            android:theme="@style/Theme.PoojaPurohit">
        </activity>

        <activity
            android:name=".bookpurohit.BookPurohitActivity"
            android:exported="false"
            android:theme="@style/Theme.PoojaPurohit">
        </activity>

        <!-- Added BookingsActivity with deep link intent filter -->
        <activity
            android:name=".booking.BookingsActivity"
            android:launchMode="singleTop"
            android:exported="true"
            android:theme="@style/Theme.PoojaPurohit">

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="poojapurohit"
                    android:host="bookings" />
            </intent-filter>
        </activity>

    </application>

</manifest>